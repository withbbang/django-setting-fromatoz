install python
install pip
install virtualenv

check each version

> cd [directory name]
> python -m venv [envname]
> cd [envname]/Scripts
> activate
(envname) > cd ../..
(envname) > django-admin startproject [projectname]
(envname) > cd projectname
(envname) > python manage.py startapp [appname]
(envname) > pip install mysqlclient (depanding on db engine)
(envname) > pip install django
(envname) > pip install djangorestframework
(envname) > pip install django-cors-headers
